// image-translator-new/next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Enable the React Compiler, a key feature of Next.js 15
    reactCompiler: false,
  },
};

export default nextConfig;
